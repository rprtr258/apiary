{
  "name": "apiary-mcp-sample-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "start:stdio": "bun run server.ts",
    "start:http": "MCP_TRANSPORT=http bun run server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.0.0"
  }
}
